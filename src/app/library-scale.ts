/** Below this count, render a plain list/grid (no virtualizer overhead). */
export const LIBRARY_VIRTUALIZE_MIN = 60;

/** Disable layout morph animations at or above this visible count. */
export const LIBRARY_SIMPLIFIED_MOTION_MIN = 80;

export const LIBRARY_GRID_MIN_COL_PX = 256;
export const LIBRARY_GRID_GAP_PX = 16;
export const LIBRARY_LIST_ROW_ESTIMATE_PX = 64;
export const LIBRARY_GRID_ROW_ESTIMATE_PX = 296;
