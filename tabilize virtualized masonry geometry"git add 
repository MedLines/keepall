[1mdiff --git a/src/app/library-main-grid.tsx b/src/app/library-main-grid.tsx[m
[1mindex b1b04c0..1f86f19 100644[m
[1m--- a/src/app/library-main-grid.tsx[m
[1m+++ b/src/app/library-main-grid.tsx[m
[36m@@ -30,7 +30,7 @@[m [mexport function LibraryMainGrid({[m
   }[m
 [m
   if (layout === "grid") {[m
[31m-    return <LibraryMasonry items={visibleItems} scopeKey={scopeKey} scrollRef={scrollRef} renderItem={renderItem} />;[m
[32m+[m[32m    return <LibraryMasonry key={scopeKey} items={visibleItems} scopeKey={scopeKey} scrollRef={scrollRef} renderItem={renderItem} />;[m
   }[m
 [m
   const useVirtualList = visibleItems.length >= LIBRARY_VIRTUALIZE_MIN;[m
[36m@@ -38,7 +38,7 @@[m [mexport function LibraryMainGrid({[m
   if (useVirtualList) {[m
     return ([m
       <LibraryVirtualItems[m
[31m-        scopeKey={scopeKey}[m
[32m+[m[32m        key={scopeKey}[m
         items={visibleItems}[m
         scrollRef={scrollRef}[m
         renderItem={renderItem}[m
[1mdiff --git a/src/app/library-masonry.tsx b/src/app/library-masonry.tsx[m
[1mindex a974844..d1d6598 100644[m
[1m--- a/src/app/library-masonry.tsx[m
[1m+++ b/src/app/library-masonry.tsx[m
[36m@@ -16,6 +16,7 @@[m [mimport {[m
   LIBRARY_GRID_GAP_PX,[m
   LIBRARY_GRID_ROW_ESTIMATE_PX,[m
   LIBRARY_VIRTUALIZE_MIN,[m
[32m+[m[32m  estimateLibraryGridItemHeight,[m
 } from "./library-scale";[m
 [m
 export type MasonryPlacement = {[m
[36m@@ -36,8 +37,18 @@[m [mexport function LibraryMasonry({ items, scrollRef, scopeKey, renderItem }: Props[m
   const [width, setWidth] = useState(0);[m
   const [scrollMargin, setScrollMargin] = useState(0);[m
   const columns = gridColumnCount(width);[m
[32m+[m[32m  const columnWidth = width[m
[32m+[m[32m    ? (width - (columns - 1) * LIBRARY_GRID_GAP_PX) / columns[m
[32m+[m[32m    : 0;[m
   const virtualized = items.length >= LIBRARY_VIRTUALIZE_MIN;[m
   const getItemKey = useCallback((index: number) => items[index].id, [items]);[m
[32m+[m[32m  const estimateSize = useCallback([m
[32m+[m[32m    (index: number) =>[m
[32m+[m[32m      items[index][m
[32m+[m[32m        ? estimateLibraryGridItemHeight(items[index], columnWidth)[m
[32m+[m[32m        : LIBRARY_GRID_ROW_ESTIMATE_PX,[m
[32m+[m[32m    [columnWidth, items],[m
[32m+[m[32m  );[m
   const rangeExtractor = useCallback((range: Parameters<typeof defaultRangeExtractor>[0]) => ([m
     virtualized[m
       ? defaultRangeExtractor(range)[m
[36m@@ -47,7 +58,7 @@[m [mexport function LibraryMasonry({ items, scrollRef, scopeKey, renderItem }: Props[m
     count: items.length,[m
     getScrollElement: () => scrollRef.current,[m
     getItemKey,[m
[31m-    estimateSize: () => LIBRARY_GRID_ROW_ESTIMATE_PX,[m
[32m+[m[32m    estimateSize,[m
     lanes: columns,[m
     // Keep each card in its column as images load; only its vertical position changes.[m
     laneAssignmentMode: "estimate",[m
[36m@@ -55,15 +66,35 @@[m [mexport function LibraryMasonry({ items, scrollRef, scopeKey, renderItem }: Props[m
     overscan: columns * 4,[m
     scrollMargin,[m
     rangeExtractor,[m
[32m+[m[32m    directDomUpdates: true,[m
[32m+[m[32m    useAnimationFrameWithResizeObserver: true,[m
[32m+[m[32m    useFlushSync: false,[m
   });[m
 [m
[32m+[m[32m  const setGridRef = useCallback((node: HTMLUListElement | null) => {[m
[32m+[m[32m    gridRef.current = node;[m
[32m+[m[32m    virtualizer.containerRef(node);[m
[32m+[m[32m  }, [virtualizer]);[m
[32m+[m
   useLayoutEffect(() => {[m
     const grid = gridRef.current;[m
     if (!grid) return;[m
[31m-    const observer = new ResizeObserver(([entry]) => setWidth(entry.contentRect.width));[m
[31m-    setWidth(grid.clientWidth);[m
[32m+[m[32m    let frame = 0;[m
[32m+[m[32m    let nextWidth = Math.round(grid.clientWidth);[m
[32m+[m[32m    const commitWidth = () => {[m
[32m+[m[32m      frame = 0;[m
[32m+[m[32m      setWidth((current) => current === nextWidth ? current : nextWidth);[m
[32m+[m[32m    };[m
[32m+[m[32m    const observer = new ResizeObserver(([entry]) => {[m
[32m+[m[32m      nextWidth = Math.round(entry.contentRect.width);[m
[32m+[m[32m      if (!frame) frame = requestAnimationFrame(commitWidth);[m
[32m+[m[32m    });[m
[32m+[m[32m    setWidth(nextWidth);[m
     observer.observe(grid);[m
[31m-    return () => observer.disconnect();[m
[32m+[m[32m    return () => {[m
[32m+[m[32m      observer.disconnect();[m
[32m+[m[32m      if (frame) cancelAnimationFrame(frame);[m
[32m+[m[32m    };[m
   }, []);[m
 [m
   // Status messages above the grid are part of the same scroll container.[m
[36m@@ -74,23 +105,14 @@[m [mexport function LibraryMasonry({ items, scrollRef, scopeKey, renderItem }: Props[m
       const margin = grid.getBoundingClientRect().top - scroll.getBoundingClientRect().top + scroll.scrollTop;[m
       if (margin !== scrollMargin) setScrollMargin(margin);[m
     }[m
[31m-  }, [scrollRef, scrollMargin, width, renderItem]);[m
[32m+[m[32m  }, [scrollRef, scrollMargin, width]);[m
 [m
   useLayoutEffect(() => {[m
     virtualizer.measure();[m
[31m-    // Width changes invalidate off-screen sizes too. Refresh mounted cards now,[m
[31m-    // even when their height stayed the same and ResizeObserver would stay silent.[m
[31m-    gridRef.current?.querySelectorAll<HTMLLIElement>(":scope > li").forEach(virtualizer.measureElement);[m
   }, [width, virtualizer]);[m
 [m
[31m-  useLayoutEffect(() => {[m
[31m-    virtualizer.scrollToOffset(0);[m
[31m-  }, [scopeKey, virtualizer]);[m
[31m-[m
[31m-  const columnWidth = (width - (columns - 1) * LIBRARY_GRID_GAP_PX) / columns;[m
[31m-[m
   return ([m
[31m-    <ul ref={gridRef} aria-label="Library items" className="relative w-full" style={{ height: virtualizer.getTotalSize() }}>[m
[32m+[m[32m    <ul ref={setGridRef} aria-label="Library items" data-masonry-scope={scopeKey} className="relative w-full">[m
       {virtualizer.getVirtualItems().map(cell => renderItem(items[cell.index], {[m
         index: cell.index,[m
         measureElement: virtualizer.measureElement,[m
[36m@@ -99,7 +121,6 @@[m [mexport function LibraryMasonry({ items, scrollRef, scopeKey, renderItem }: Props[m
           top: 0,[m
           insetInlineStart: cell.lane * (columnWidth + LIBRARY_GRID_GAP_PX),[m
           width: width ? columnWidth : "100%",[m
[31m-          transform: `translateY(${cell.start - scrollMargin}px)`,[m
         },[m
       }))}[m
     </ul>[m
[1mdiff --git a/src/app/library-scale.test.ts b/src/app/library-scale.test.ts[m
[1mindex 0295f47..7afd409 100644[m
[1m--- a/src/app/library-scale.test.ts[m
[1m+++ b/src/app/library-scale.test.ts[m
[36m@@ -1,5 +1,6 @@[m
 import { describe, expect, test } from "vitest";[m
 import {[m
[32m+[m[32m  estimateLibraryGridItemHeight,[m
   gridColumnCount,[m
   LIBRARY_VIRTUALIZE_MIN,[m
 } from "./library-scale";[m
[36m@@ -14,4 +15,44 @@[m [mdescribe("library-scale", () => {[m
     expect(gridColumnCount(800)).toBe(2);[m
     expect(gridColumnCount(1200)).toBe(3);[m
   });[m
[32m+[m
[32m+[m[32m  test("estimates cards from their visible type instead of one fixed row height", () => {[m
[32m+[m[32m    const common = {[m
[32m+[m[32m      createdAt: 1,[m
[32m+[m[32m      updatedAt: 1,[m
[32m+[m[32m      tagIds: [],[m
[32m+[m[32m      collectionIds: [],[m
[32m+[m[32m    };[m
[32m+[m[32m    const shortNote = estimateLibraryGridItemHeight({[m
[32m+[m[32m      ...common,[m
[32m+[m[32m      id: "short",[m
[32m+[m[32m      type: "note",[m
[32m+[m[32m      title: "Small note",[m
[32m+[m[32m      content: "One line.",[m
[32m+[m[32m    }, 360);[m
[32m+[m[32m    const longNote = estimateLibraryGridItemHeight({[m
[32m+[m[32m      ...common,[m
[32m+[m[32m      id: "long",[m
[32m+[m[32m      type: "note",[m
[32m+[m[32m      title: "Long note",[m
[32m+[m[32m      content: "A longer thought. ".repeat(40),[m
[32m+[m[32m    }, 360);[m
[32m+[m[32m    con